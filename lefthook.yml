pre-commit:
  commands:
    biome-check:
      glob: "*.{ts,mjs,js,json,astro}"
      run: npx biome check --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    conventional-commit:
      run: npx commitlint --edit {1}
